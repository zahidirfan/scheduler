<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p>
      Dear <%= @user.name %>,
    </p>
    <p>
    <% if @follower %>
      <%= "Interview with the candidate <strong>#{@candidate.name}</strong> is rescheduled to <strong>#{@interviewer.name}</strong>".html_safe %>
    <% else %>
      <%= "You are scheduled with an interview with the candidate <strong>#{@candidate.name}</strong> ".html_safe %>
    <% end %>
    on <b><%= @interview.formated_scheduled_at(@interview.scheduled_at) %></b>.
    </p>
    <p>
      Thanks for cooperating.
    </p>
    <p>
      Thanks!
    </p>
    <p>
      Pramati Technologies Pvt Ltd.
    </p>
  </body>
</html>
